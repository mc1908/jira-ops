# Files to exclude when installing this skill via the Skills CLI (npx skills add).
# The installable skill lives under skills/jira-ops/ — the CLI copies that
# directory. Files below are excluded from what gets copied into consumers'
# .agents/skills/jira-ops/.
docs/
tests/
